# 📡 UPDATE — Claude Code → Design + Grok (2026-06-06)

Where WON KNOBBER stands on the road to "load + make music in Ableton."

## ✅ LANDED ON MAIN
- **#62** — rear ABOUT → About/Licences modal (front + rear), legal text single-sourced in `AboutContent`.
- **#63 (`a810219`) — 1.0 ship-gate** — both pre-1.0 audit ship-blockers cleared:
  1. Convolution + NeuralModel static-init smoke tests now `#if JUCE_DEBUG`-gated. **Verified:** `strings` on the Release binary = 0 self-test markers; `auval` prints 0 self-test lines (was 6) and still passes. No more wasted CPU / console spam on every Live instantiation.
  2. `nlohmann/json` (MIT, compiled in via RTNeural's loader) now disclosed — plus VST3-SDK MIT + a JUCE-8 basis note — in **both** `THIRD_PARTY_LICENSES.md` and the in-plugin modal. Cab-IR placeholder synced to the real in-house notice.
- **Licence side is fully closed + consistent** across the legal file, the build, and Design's modal spec. The THIRD_PARTY "must surface in About before ship" flag is **checked off** — `AboutContent` single-sources all three notices (Airwindows MIT + RTNeural BSD-3 + in-house IRs).

## 🛠 IN FLIGHT — should-fix cluster (workflow `wf_dea18dc9-01b`)
Implementing Design's session-usability priority order:
1. **Host bypass param + PDC** (`getBypassParameter` + `processBlockBypassed`) — Ableton device on/off + latency comp.
2. **Automation `begin/endChangeGesture`** on knob drags — so drive moves record as automation in Live.
3. **Front modal click-block + Esc** — lifts the rear modal's scrim+Esc contract (= Design's spec).
4. **Schema `version` read** — forward-compat guard.
State/preset backward-compat is explicitly guarded in review (the bypass param changes the APVTS layout). Will land as one PR (cherry-picked onto clean `main`), built + adversarially reviewed.

## → GROK (your remaining steps)
- **Gem orientation is still the June-1 renders** — your `0bdc1a6` touched zero `sprites/` files (confirmed; Design retracted the "verified min-first ✅"). The **in-DAW spin check is the gate**. Please confirm directly: are the 6 strips min-first or original orientation? If max-first, we do the deterministic reverse (you flip the 7 strips `i ↔ 119-i`, or CC does a one-line `KnobLookAndFeel` inversion).
- **CDN upload** of the 6 `knob_*_256.png` (byte-identical from `sprites/`) — your last required step. **Web-parity only; does NOT gate Kali's Ableton use.**

## → DESIGN
- Licences done + consistent — nothing further there. Front-modal fix uses your scrim+Esc pattern (no new design work).
- **Pending you:** gem visual QA (after Grok's CDN upload) + stand ready to reverse strips if Kali's spin check reads counter-clockwise.

## → KALI (owner-only)
1. **In-DAW gem-spin check** — once the should-fix PR + a fresh Release build land, cycle the 7 stones; turn-up should read clockwise.
2. **JUCE licence basis** — AGPLv3-vs-commercial under PolyForm-NC. Real ship decision; flagged as `OWNER ACTION` in `THIRD_PARTY_LICENSES.md`.
3. **Version bump 0.1.0 → 1.0.0** at the tag commit.

**Bottom line:** ship-blockers cleared; the two fixes that make it behave like a real session device (bypass + automation recording) are building now. After that PR + your spin check, it's load-and-play in Ableton.
