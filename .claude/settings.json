{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          { "type": "command", "command": "f=$(jq -r '.tool_input.file_path // empty' 2>/dev/null); case \"$f\" in *.cpp|*.cc|*.cxx|*.mm|*.h|*.hpp) command -v clang-format >/dev/null && clang-format -i \"$f\";; esac" }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          { "type": "command", "command": "./scripts/verify.sh" }
        ]
      }
    ]
  }
}
