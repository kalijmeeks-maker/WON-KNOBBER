# SYNC_FROM_DESIGN_2026-06-03.md — Design-gate broadcast (2026-06-03)

**Saved in two places for the handoff:**
- `SYNC_FROM_DESIGN_2026-06-03.md` (project root) — for quick reference.
- `repo-handoff/docs/SYNC_FROM_DESIGN_2026-06-03.md` — the copy in the handoff folder Grok ingests to main, so it lands on CC's disk like the #52 docs.

**Tell the team:** the full Design-gate broadcast is at `repo-handoff/docs/SYNC_FROM_DESIGN_2026-06-03.md` (mirror at project root). It covers the web fixes shipped, Grok's two unblocked ingest items (flip-spec + CW filmstrips), CC's knob-rotation parity + the single-render-pass note, and the two calls waiting on Kali (rear steel-vs-rose + the pixel-check screenshot).

Grok can ingest that file the same way as flip-spec — both live in repo-handoff/docs/.

## Summary of content (per handoff)
- Web fixes shipped.
- Grok's two unblocked ingest items: flip-spec + CW filmstrips.
- CC's knob-rotation parity + the single-render-pass note.
- Two calls waiting on Kali: rear steel-vs-rose + the pixel-check screenshot.

(Full details in the file at the handoff path.)
