# WON-KNOBBER — Status (2026-06-02, pm): PR #38 complete + live-verified

Bypass dim-state + About panel are **done and verified in Ableton Live**. Per-agent
breakdown below so Design and Grok can move forward.

## → Claude Design

- **Bypass §3 dim-state: shipped + confirmed in-DAW.** Every token from
  `bypass-dimstate-tokens.json` (#46) wired verbatim and verified live — panel cool-wash,
  POWER/SIG/CLIP dark, transfer + harmonics scopes **frozen at 25%**, dB readout dull
  ember, drive **and** mix value-arcs dulled, gem cooled to a cold stone, and the critical
  exception confirmed: **I/O meters keep moving in grey**.
- **About card: verbatim §3.** Exact credit block (Airwindows MIT © Chris Johnson / IRs
  under their licences / Neural models © Kali Meeks / WON KNOBBER © 2026 Kali Meeks ·
  PolyForm Noncommercial 1.0.0 · JUCE 8 · VST3™ Steinberg), hairline + amber
  "View full licences ▸", ~60% modal over the dimmed face.
- **Two bugs the live verify caught + fixed**, both on the DRY/WET knob (no filmstrip → an
  ellipse with two separate amber elements): the value-arc (`0d2dbee`) and the ellipse
  **edge ring** (`1719b11`). Both now de-energize. The edge ring was caught by Kali's human
  eyes in the actual DAW.
- **Deferred polish (flag if you want them sooner):** 180 ms animated transition + true
  filmstrip gem-desaturation (currently a cool overlay approximation).
- **Owed by Design when unblocked:** the "modified-from-preset" indicator visual (CC is
  leaving the `isDirty` hook in the preset bake); the rear bypass-token extension + the
  front flip-affordance spec — all waiting on Kali locking rear-fold.

## → Grok (+ agents)

- The bypass work was **entirely front-face GUI — zero impact on the asset/research lane.**
  Keep IR captures + RTNeural training running (same six filenames, `neuralModel` naming,
  ≤ 350 KB), per-asset license notices for the licences scroll.
- **CC is building PR 3 (RTNeural) next**, and the one thing that will block neural from
  ever engaging: **the 4 model stubs use a custom JSON schema RTNeural's `json_parser`
  can't read.** Re-export the stubs **and** the real trained models in **RTNeural-native
  JSON** so CC's `NeuralModel` loads them.
- Keep the manifest pointed at the authoritative voice map + the pinned RTNeural SHA.

## → Claude Code (me), next

Finish **PR 3 (RTNeural)** → **preset bake** (machine ids, `isDirty` / modified-dot hook,
override precedence per #46). **Holding PR 4** until Kali locks rear-fold.

## Gates still on Kali

- **Lock rear-fold** → unblocks PR 4 + Design's bridge/bake.
- **#40 nod** → flatten the stack: `#41 → #40 → #42 → #44 → #45 → #46 → #38`.

## Open PRs

| PR | What | State |
|----|------|-------|
| #38 | bypass dim-state + About | **complete, live-verified** (tip `1719b11`) |
| #41 | WonKnobberState cab/neural fields | open |
| #42 | embed 6 IRs + zero-latency Convolution | open (stacked on #41+#40) |
| #44 | rear-panel design handoff | open |
| #45 | long-run assignments doc | open |
| #46 | dim-state tokens + override docs | open |
| #40 | Grok cab/neural assets | open (Kali nod) |
| PR 3 | RTNeural neural stage | in progress |
